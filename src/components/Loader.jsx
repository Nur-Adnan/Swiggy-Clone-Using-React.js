import React from "react";
import "./LoaderLayout.css"; // Import CSS for the loader

const LoaderLayout = () => {
  return (
    <section className="loader-section">
      <div className="loader-spinner"></div>
    </section>
  );
};

export default LoaderLayout;
